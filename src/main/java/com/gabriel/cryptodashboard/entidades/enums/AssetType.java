package com.gabriel.cryptodashboard.entidades.enums;

public enum AssetType {
    CRYPTO, // Bitcoin, Ethereum, Solana
    FIAT,   // Real, Dólar, Euro
    STOCK   // Ações (caso queira expandir no futuro)
}